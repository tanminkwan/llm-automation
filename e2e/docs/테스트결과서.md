# `e2e` — 테스트 결과서

| 항목 | 값 |
|---|---|
| 모듈/Phase | `e2e` (Phase 6) |
| 실행 일시 | 2026-04-20 KST |
| 브랜치 | `phase6` |
| 관련 문서 | `요구사항.md`, `설계서.md`, `docs/P06_설계서.md` |

---

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| OS | Linux 6.8.0-107-generic (Ubuntu) |
| Python | 3.12.3 (uv managed) |
| 주요 의존성 | trigger-core (workspace), celery 5.4+, pytest 9.0+, jsonschema 4.22+ |

## 2. Offline 테스트 (stack/OpenAI 불필요)

```bash
cd e2e
uv run pytest tests -m "not e2e"
```

| 항목 | 결과 |
|---|---|
| 테스트 케이스 수 | 8 |
| 통과 / 실패 / 스킵 | 8 / 0 / 0 |
| 실행 시간 | 0.44s |

### 테스트 구성

- `test_fixtures.py` (5)
  - scenario_A trigger.json → `TriggerEvent` 파싱
  - scenario_A before 트리 존재 확인
  - scenario_B trigger.json → `TriggerEvent` 파싱
  - report.schema.json Draft-07 스키마 유효성
  - 스키마가 필수 필드 누락을 탐지
- `test_emit_trigger.py` (3)
  - CLI → scenario_A → `comment.process` / `comment_queue`
  - CLI → scenario_B → `configaudit.process` / `configaudit_queue`
  - 존재하지 않는 파일 → exit code 2

## 3. E2E 테스트 (stack + OpenAI 실제 실행, 2026-04-20)

`infra/docker-stack.test.yml` 배포 후 `E2E_ENABLED=1` 로 실제 OpenAI 호출 포함 실행.

```bash
# 사전: 10개 서비스 모두 1/1 상태 (redis, qdrant, gitea, flower, leebalso-mock,
#      rag-mcp, configaudit-mcp, flask-webhook, celery-comment, celery-configaudit)
set -a && . infra/.env && set +a
export CELERY_BROKER_URL=redis://localhost:6379/0
export CELERY_RESULT_BACKEND=redis://localhost:6379/1
cd e2e && uv run --project . pytest tests -v
```

| 항목 | 결과 |
|---|---|
| 테스트 케이스 수 | 10 (offline 8 + E2E 2) |
| 통과 / 실패 / 스킵 | 10 / 0 / 0 |
| 실행 시간 | 약 25s (OpenAI 응답 포함) |

| 케이스 | 상태 | 비고 |
|---|---|---|
| `test_scenario_a_java_comment_roundtrip` | **PASS** | `Calculator.java` 에 OpenAI 생성 Javadoc 블록 5개 삽입, `FIXTURE_RESULT_DIR/main/<ts>/` 스냅샷 + `manifest.json` 확인 |
| `test_scenario_b_configaudit_report` | **PASS** | `configaudit-mcp` 3-way diff → `o4-mini` 구조화 분석 → JSON sidecar + Markdown 동시 기록, Draft-07 스키마 통과 |

### 3.1 실제 출력 예시

**시나리오 A** — `Calculator.java` 의 첫 클래스 블록:

```java
/**
 * The Calculator class provides basic methods for arithmetic operations
 * such as addition, subtraction, multiplication, and division.
 */
public class Calculator {
```

**시나리오 B** — `AnalysisReport.json` summary 발췌:

```text
1. All environments (dev, stage, prod) have increased MaxClients, Timeout,
   and added KeepAlive On.
2. LogLevel has been made more verbose in each environment: dev warn→debug,
   stage warn→info, prod error→warn.
3. Stage's Timeout (45s) does not follow the ascending pattern seen in
   dev (60s) and prod (60s), potentially impacting request handling.
```

### 3.2 실행 중 해결한 이슈

상세는 `docs/P06_테스트결과서.md §4.1` 참조 (9건):
stack workspace wheel 빌드, `python -m <module>` CMD, `curl` 부재 healthcheck,
single-node label 중복 키, `LEEBALSO_BASE_URL` 누락, `case = work_id` 버그,
JSON sidecar 누락, bind-mount 경로 정합 등.

## 4. 린트

| 도구 | 결과 |
|---|:---:|
| `ruff check .` | PASS (All checks passed!) |
| `ruff format --check .` | PASS |

## 5. DoD 충족

- [x] E2E-FR-01 trigger.json 스키마 (scenario_A/B 모두)
- [x] E2E-FR-02 fixture 소스 트리 (Java Calculator.java)
- [x] E2E-FR-03 CLI 발행 (`bin/emit_trigger.py`)
- [x] E2E-FR-04 stack 기동/정리 스크립트
- [x] E2E-FR-05 시나리오 A 검증 코드 (실제 OpenAI 경유 PASS)
- [x] E2E-FR-06 시나리오 B 검증 코드 (실제 OpenAI 경유 PASS + Draft-07 스키마)
- [x] E2E-FR-07 offline 검증 PASS (8/8)
- [x] E2E-FR-08 `trap` 기반 teardown (Makefile `e2e` target)
- [x] E2E-NFR-01~NFR-03 충족
- [x] E2E-NFR-04 실제 시나리오 — 두 시나리오 합쳐 약 25s (5분 목표치 대비 대폭 여유)
