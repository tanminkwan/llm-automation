# `flask-webhook` — 테스트 결과서

| 항목 | 값 |
|---|---|
| 모듈/Phase | `services/flask-webhook` (Phase 6 리팩토링) |
| 실행 일시 | 2026-04-20 KST |
| 브랜치 | `phase6` |
| 관련 문서 | `요구사항.md`, `설계서.md`, `docs/P06_설계서.md` |

---

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| OS | Linux 6.8.0-107-generic (Ubuntu) |
| Python | 3.12.3 (uv managed) |
| 주요 의존성 | flask 3.0+, celery 5.4+, pydantic 2.7+, pydantic-settings 2.4+, **trigger-core (workspace)** |

## 2. 실행 명령

```bash
cd services/flask-webhook
uv run pytest --cov=src/flask_webhook --cov-branch --cov-report=term-missing
uv run ruff check src tests
uv run ruff format --check src tests
uv run mypy src
```

## 3. 결과 요약

| 항목 | 결과 |
|---|---|
| 테스트 케이스 수 | 12 |
| 통과 / 실패 / 스킵 | 12 / 0 / 0 |
| 실행 시간 | 0.34s |

> Phase 5 대비 케이스 수 감소(22 → 12): 도메인 테스트(HMAC, 분류, 모델, 멱등 캐시) 10개가 `libs/trigger-core/tests/` 로 이관 — 중복 제거.

## 4. 커버리지

| 파일 | Stmts | Branch | Cover |
|---|---:|---:|---:|
| `src/flask_webhook/__init__.py` | 3 | 0 | 100% |
| `src/flask_webhook/app.py` | 38 | 6 | 100% |
| `src/flask_webhook/settings.py` | 7 | 0 | 100% |
| **TOTAL** | **48** | **6** | **100%** |

기준 (≥ 95%) 대비: **PASS**.

## 5. 린트 / 타입체크

| 도구 | 결과 |
|---|:---:|
| `ruff check` | PASS (All checks passed!) |
| `ruff format --check` | PASS (8 files already formatted) |
| `mypy src` (strict) | PASS (Success: no issues found in 3 source files) |

## 6. DoD 충족 여부

- [x] FR-01 Webhook 수신 — `test_app::test_success`
- [x] FR-02 HMAC 검증 — `test_app::test_invalid_hmac` (+ trigger-core HMAC 테스트)
- [x] FR-03 work_type 판별 — `test_app::test_configaudit_priority`, `test_skipped_no_matching_files` (+ trigger-core 분류 테스트)
- [x] FR-04 Celery task 발행 — `test_app::test_success`, `test_configaudit_priority`
- [x] FR-05 멱등키 — `test_app::test_duplicate_delivery`
- [x] FR-06 헬스체크 — `test_app::TestHealth::test_health`
- [x] FR-07 TriggerSource/Dispatcher DI — `test_app::test_dispatcher_override`
- [x] FR-08 public API 축약 — `test_public_api::test_public_exports`, `test_trigger_core_is_source_of_truth`
- [x] NFR-01~NFR-06 모두 충족
