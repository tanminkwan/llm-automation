# `celery-comment` — 설계서

| 항목 | 값 |
|---|---|
| 모듈 | `services/celery-comment` |
| 선행 문서 | `services/celery-comment/docs/요구사항.md` |
| 상태 | 확정 |
| 작성일 / 최종 갱신일 | 2026-04-17 / 2026-04-17 |

---

## 1. 디렉터리 구조

```
services/celery-comment/
├── src/celery_comment/
│   ├── __init__.py
│   ├── app.py              # Celery app 인스턴스
│   ├── task.py             # comment.process task
│   ├── git_ops.py          # Git clone/commit/push 추상화
│   ├── protocols.py        # GitClient Protocol
│   ├── prompt.py           # Agent 프롬프트 빌더
│   ├── models.py           # TaskPayload 등
│   └── settings.py         # CommentSettings
└── tests/
```

## 2. 핵심 모듈

### 2.1 Protocols

```python
class GitClient(Protocol):
    def clone(self, url: str, dest: Path, ref: str) -> None: ...
    def add_commit_push(self, repo_dir: Path, message: str, branch: str) -> None: ...
```

### 2.2 `task.py`

```python
@celery_app.task(name="comment.process", queue="comment_queue")
def process_comment(
    work_id: str, repo_url: str, clone_url: str, ref: str, changed_files: list[str]
) -> dict[str, Any]:
    """1. clone → 2. AgentRunner.run() → 3. commit/push → 4. cleanup"""
```

### 2.3 `git_ops.py`

```python
class SubprocessGitClient:
    """subprocess 기반 Git 클라이언트 — GitClient Protocol 구현."""
    def clone(self, url: str, dest: Path, ref: str) -> None: ...
    def add_commit_push(self, repo_dir: Path, message: str, branch: str) -> None: ...
```

## 3. 설정 항목

| 키 (env) | 의미 | 기본값 | 필수 | 민감 |
|---|---|---|---|---|
| `CELERY_BROKER_URL` | Redis broker | `redis://localhost:6379/0` | N | N |
| `CELERY_RESULT_BACKEND` | Redis result | `redis://localhost:6379/1` | N | N |
| `COMMENT_WORK_DIR` | 임시 작업 디렉터리 | `/tmp/celery-comment` | N | N |
| `RAG_MCP_URL` | RAG MCP 서비스 URL | `http://localhost:9001` | N | N |
| `AGENT_RUNNER_KIND` | AgentRunner 종류 | `opencode` | N | N |
| `AGENT_TIMEOUT_SECONDS` | Agent 실행 타임아웃 | `300` | N | N |
| `GIT_COMMIT_AUTHOR` | 커밋 author | `AI Bot <ai-bot@local>` | N | N |

## 4. 테스트 전략

| ID | 대상 | 케이스 |
|---|---|---|
| T-01 | `process_comment` | happy path — FakeRunner + FakeGit |
| T-02 | `process_comment` | AgentRunner 실패 → 예외 전파 |
| T-03 | `process_comment` | 변경 파일 없음 → skip commit |
| T-04 | `SubprocessGitClient.clone` | clone 성공 (fake subprocess) |
| T-05 | `SubprocessGitClient.add_commit_push` | commit/push 성공 |
| T-06 | `build_prompt` | 프롬프트 생성 확인 |
| T-07 | `TaskPayload` | 모델 파싱 |
| T-08 | Settings | env 주입 |
| T-09 | Settings | 기본값 |
| T-10 | Protocol | FakeGitClient 만족 |
| T-11 | public API | import 확인 |
