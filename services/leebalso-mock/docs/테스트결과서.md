# `leebalso-mock` — 테스트 결과서

| 항목 | 값 |
|---|---|
| 모듈/Phase | `services/leebalso-mock` (Phase 2) |
| 실행 일시 | 2026-04-16 KST |
| 커밋 / 브랜치 | `phase2` 작업 트리 |
| 실행자 | 로컬 개발 환경 |
| 관련 문서 | `요구사항.md`, `설계서.md` |

---

## 1. 실행 환경

| 항목 | 값 |
|---|---|
| OS | Linux 6.8.0-107-generic (Ubuntu) |
| Python | 3.12.3 (uv managed) |
| uv | 0.11.6 |
| 주요 의존성 | fastapi 0.115+, uvicorn 0.44, pydantic 2.13, pydantic-settings 2.13 |

## 2. 실행 명령

```bash
uv sync --all-packages
make -C services/leebalso-mock lint
make -C services/leebalso-mock coverage
make -C services/leebalso-mock build
```

## 3. 결과 요약

| 항목 | 결과 |
|---|---|
| 테스트 케이스 수 | 19 |
| 통과 / 실패 / 스킵 | 19 / 0 / 0 |
| 실행 시간 | 0.63s |

| 파일 | 건수 | 커버 케이스 |
|---|---:|---|
| `tests/test_models.py` | 2 | T-01, T-02 |
| `tests/test_settings.py` | 2 | T-03, T-04 |
| `tests/test_loader.py` | 6 | T-05 ~ T-10 |
| `tests/test_app.py` | 8 | T-11 ~ T-18 |
| `tests/test_public_api.py` | 1 | T-19 |
| **합계** | **19** | — |

## 4. 커버리지

| 항목 | 수치 | 기준 | 판정 |
|---|---:|---:|:---:|
| 라인 커버리지 | 100% (69/69) | ≥ 95% | ✅ |
| 브랜치 커버리지 | 100% (6/6) | ≥ 95% | ✅ |

```
Name                            Stmts   Miss Branch BrPart  Cover   Missing
---------------------------------------------------------------------------
src/leebalso_mock/__init__.py       5      0      0      0   100%
src/leebalso_mock/app.py           24      0      2      0   100%
src/leebalso_mock/loader.py        22      0      4      0   100%
src/leebalso_mock/models.py        10      0      0      0   100%
src/leebalso_mock/settings.py       8      0      0      0   100%
---------------------------------------------------------------------------
TOTAL                              69      0      6      0   100%
```

## 5. 95% 미달 시 사유

해당 없음. 라인/브랜치 모두 100%.

## 6. 린트 / 타입체크

| 도구 | 결과 | 비고 |
|---|:---:|---|
| `ruff check src tests` | ✅ | All checks passed |
| `ruff format --check src tests` | ✅ | 12 files already formatted |
| `mypy src` (strict) | ✅ | no issues found in 5 source files |

## 7. 컨테이너 이미지

> Docker 이미지 빌드는 infra 스택 배포 시 검증 예정. wheel 빌드 확인 완료.

```bash
$ make build
# → dist/leebalso_mock-0.1.0-py3-none-any.whl 생성 확인
```

## 8. DoD 충족 여부

- [x] FR-01 `GET /config/httpm` — T-11, T-12, T-17
- [x] FR-02 결정성 보장 — T-08, T-16
- [x] FR-03 픽스처 기반 응답 — T-05
- [x] FR-04 env 누락 → 422 — T-13
- [x] FR-05 미지 case → 404 — T-06, T-14
- [x] FR-06 미지 env → 404 — T-07, T-15
- [x] FR-07 `GET /health` — T-18
- [x] FR-08 설정 주입 — T-03, T-04
- [x] NFR-01 라인 커버리지 100%
- [x] NFR-02 브랜치 커버리지 100%
- [x] NFR-03 mypy strict 통과
- [x] NFR-04 ruff check + format-check 통과
- [x] NFR-07 하드코딩 없음

### `sample.env` ↔ 설계서 설정 표 일치

- `sample.env` 3개 키 ↔ 설계서 §6 설정 항목 표 — 1:1 일치 확인.

## 9. 회귀 / 기지 결함

1. **ruff format 미일치** — `loader.py`, `conftest.py` 2개 파일 포맷 불일치. `make format` 으로 해결.

미해결 이슈: 없음.

## 10. 첨부

- 재현: §2 명령으로 가능.
- fixture 파일: `fixtures/case-001/{dev,stage,prod}.{before,after}.httpm` 6개.
